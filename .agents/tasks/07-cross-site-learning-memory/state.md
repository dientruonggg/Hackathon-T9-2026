# Execution state

2026-09-17 — Phase 1: the v1 marker record remains unchanged. Repository reads expose a local catalog; marker mutations use one exclusive lock. Memory typecheck and 7 memory tests pass. Extension typecheck exposed the expected test fake missing the new catalog method; Phase 2 will update that fake. No browser verification yet.
