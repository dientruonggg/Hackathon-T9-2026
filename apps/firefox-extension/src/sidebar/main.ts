// TODO(Agent 1): implement sidebar state machine and render the three marker actions.
const app = document.querySelector<HTMLElement>("#app");

if (app) {
  app.textContent = "TODO: Agent 1 — Firefox sidebar";
}
